package com.modsen.software.passenger.validation;

/* This interface is used as marker for validation to
   disable id validation for create request and keep it
   for update request
 */
public interface OnUpdate {
}
