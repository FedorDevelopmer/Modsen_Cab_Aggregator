package com.modsen.software.driver.validation;

/* This interface is used as marker for validation
   of id value when it's update request and to
   disable validation while creating new object
 */
public interface OnUpdate {
}
